# Host running the Laravel API. The dev server proxies /api to it, so the panel
# stays same-origin and never needs CORS configured on the backend.
#
# php artisan serve      -> http://127.0.0.1:8000
# XAMPP virtual host     -> http://ivoirequiz.test
VITE_API_TARGET=http://127.0.0.1:8000
